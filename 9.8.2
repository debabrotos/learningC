#include <stdio.h>
#include <string.h>
int main()
{
    char s[1002], ch ;
    int i, j, length, is_word_started ; 
    gets(s);
    length = strlen(s);
    is_word_started = 0;
    printf("%u", length);
    printf("\n");
    return 0 ;
}