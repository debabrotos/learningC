#include<stdio.h>
int main()
{
    int u,a,t,v,u_plus_at, two_t,distance ;
    u= 10;
    a= 100;
    t=1;
    v=20;
    two_t= (t*2);
    u_plus_at = (u+a*t);
    distance = (two_t * u_plus_at);
    printf(distance);
}
